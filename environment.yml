name: sparclink
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - requests
  - nodejs
  - yarn
  - pip:
    - pyrebase
    - pandas
    - tqdm
    - python-dotenv
    - serpapi
    - google-search-results
prefix: ./env
